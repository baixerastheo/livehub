# TODO

## CI/CD
- [ ] Réécrire `review-app.yml` → `ci.yml` (lint + typecheck + tests + build, trigger sur toutes les PRs, sans label gate)
- [ ] Créer `ci-electron.yml` (typecheck + build electron, trigger sur PR vers `develop`/`main`)
